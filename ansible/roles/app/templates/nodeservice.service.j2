# nodeservice.service.j2
[Unit]
Description=Simple Node.js App
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory={{ app_dir }}
ExecStart=/usr/bin/node {{ app_dir }}/index.js
Restart=always

[Install]
WantedBy=multi-user.target